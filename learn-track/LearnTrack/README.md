# LearnTrack – Console Student & Course Management (Core Java)

The project focuses on: 

- Java basics: variables, data types, control flow
- Classes, objects, and constructors (default, parameterized, overloaded)
- Static vs instance members
- OOP principles: encapsulation, basic inheritance, polymorphism
- Collections using `ArrayList`
- Basic exception handling with `try-catch` and custom exceptions
- Clean, readable, modular code (entities, services, UI separated)

All data is stored **in memory** using `ArrayList` (no database or files).

---

## 🚀 Run in IntelliJ (10 Steps)

1. **Open IntelliJ IDEA** (Community/Ultimate)
2. **File → Open** → Select project folder (with `src/`)
3. **Wait for indexing** (bottom status bar turns green)
4. **File → Project Structure** (Ctrl+Alt+Shift+S) 
5. **Project → Project SDK** → Select **JDK 17+** → Apply
6. **Project view** → Right-click `src` → **Mark as Sources Root** (turns blue)
7. **Navigate**: `src/com/airtribe/learntrack/ui/Main.java`
8. **Verify**: `public static void main(String[] args)` exists
9. **Click green ▶️ Run** next to `main()` **OR** Shift+F10
10. **Console opens** → Use menu: "1. Add Student", etc. ✅

**Re-run**: Green ▶️ button anytime.

---